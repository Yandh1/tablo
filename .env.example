# Local-development defaults only. Do not reuse these values in a shared or
# production environment, and never commit a populated .env file.
POSTGRES_DB=tablo
POSTGRES_USER=tablo
POSTGRES_PASSWORD=tablo_local_dev
POSTGRES_PORT=5432
DATABASE_URL=postgresql://tablo:tablo_local_dev@localhost:5432/tablo
